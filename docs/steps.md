Viewed black_scholes.cpp:23-47

Based on the current state of the platform (where the orchestration plane, PostgreSQL DB, task queue, and Next.js frontend are fully operational and communicating with a mock server), the recommended next steps focus on **bringing the native C++ engine online** and **bridging the ML research layer**:

---

### Step 1: Establish the C++ Build System (CMake + vcpkg)
* **Goal:** Create a compilation pipeline for the `cpp_engine` code.
* **Actions:**
  1. Add a `CMakeLists.txt` file to the `cpp_engine` directory to define targets for the backtest engine and gRPC services.
  2. Set up `vcpkg` to manage C++ dependencies on Windows (specifically `grpc`, `protobuf`, `zeromq`, and `libtorch`).
  3. Compile the C++ entry points:
     - `entry_backtest.cpp` (for offline historical replays)
     - `entry_live_grpc.cpp` (the gRPC server wrapper implementing `StartBacktest` and `SendCommand`)

### Step 2: Implement Real Black-Scholes Backtesting in C++
* **Goal:** Connect the compiled Black-Scholes utilities to the daily options Parquet data.
* **Actions:**
  1. Ensure the compiled `entry_backtest` binary reads daily files like `20101015.parquet` generated by the converter.
  2. Implement options Greeks and implied volatility extraction on every historical data tick.
  3. Validate that backtests run with 100% determinism (reproducing identical results on repeat runs).

### Step 3: Swap Python Orchestrator to the C++ Engine
* **Goal:** Connect the FastAPI backend to the compiled C++ gRPC server.
* **Actions:**
  1. Start the compiled C++ engine on port `50051`.
  2. Modify `start.ps1` to launch the compiled C++ executable instead of `mock_grpc_server.py`.
  3. Verify that the Next.js Web UI receives and visualizes deterministic real-world telemetry from the C++ engine.

### Step 4: Hook Up the ML Workspace & LibTorch Inference
* **Goal:** Load trained PyTorch neural networks into the C++ engine for deep hedging.
* **Actions:**
  1. Train neural network hedging policies in PyTorch.
  2. Serialize them as TorchScript JIT modules (`.pt` files).
  3. Configure the C++ `StartBacktest` routine to load these models using LibTorch and run inference alongside the analytical Black-Scholes baseline for real-time model comparison.